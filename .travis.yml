sudo: false
language: python
python:
  - "3.6"


install:
  - pip3 install pipenv
  - pipenv install --dev

# for backwards-compatibility with python 3.5 and below 'py.test'
script:
  - pipenv run py.test tests
